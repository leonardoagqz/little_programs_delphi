program TamanhoEscada;

{$APPTYPE CONSOLE}

{$R *.res}
Function Sin(x: Extended): Extended;
uses
  System.SysUtils,
  System.Math;

var
angulo, seno : real;
begin
  Writeln('Valor angulo ');
  Readln(angulo);
  seno :=  Sin(DegToRad(angulo));

  Writeln('Seno = ', seno );
  Readln;


end.
